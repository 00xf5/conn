# Copy to deploy/.env before running ./setup-vps.sh on your VPS.
DOMAIN=remote.example.com
VPS_PUBLIC_IP=203.0.113.10
TURN_SECRET=generate-a-long-random-string-at-least-32-chars
# Main Admin login at https://DOMAIN/admin/
CONNECT_ADMIN_TOKEN=generate-another-long-random-admin-token
