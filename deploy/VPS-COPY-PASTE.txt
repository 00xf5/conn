=== OPEN THIS FILE IN CURSOR — select line, Ctrl+C, paste in VPS SSH ===

cd ~/connect/deploy
git pull
cp .env.example .env
nano .env

(set DOMAIN, VPS_PUBLIC_IP, TURN_SECRET, CONNECT_ADMIN_TOKEN — Ctrl+O save, Ctrl+X exit)

bash recover-vps.sh

=== ONE LINE AT A TIME if nano is easier ===

cd ~/connect/deploy

git pull

bash recover-vps.sh

=== AFTER .env IS FIXED — verify ===

docker compose ps

curl -sS https://worthyjoin.online/api/health
